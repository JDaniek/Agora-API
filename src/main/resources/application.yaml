ktor:
  application:
    modules:
      - backend.ApplicationKt.module
  deployment:
    port: 8080

# 'jwt' debe estar al nivel raíz, no dentro de 'security:'
jwt:
  secret: "super-secret-change-me-in-production"
  issuer: "agora-api-issuer"
  audience: "agora-api-audience"
  realm: "AgoraAPI"
  expirationMinutes: 60 # 1 hora

db:
  url: "jdbc:postgresql://localhost:5432/agora_db"
  user: "postgres"
  password: "Jeselmorzu08"